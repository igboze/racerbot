{
  "name": "@racerbot/contract",
  "version": "1.0.0",
  "private": true,
  "main": "src/lib.rs",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release --target-dir ../../.cargo-target",
    "test": "cargo test --target-dir ../../.cargo-target",
    "deploy": "near deploy router.racerbot.near ../../.cargo-target/wasm32-unknown-unknown/release/racerbot_router.wasm"
  }
}